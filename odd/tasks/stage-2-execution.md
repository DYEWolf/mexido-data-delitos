# Stage 2 execution

Status: done — Etapa 2 cerrada 2026-09-24 (ver `reports/stage-2-c07-closure.md`)

Source spec: `SPEC_JALISCO_STAGE_2.md`

## Cambio de método (2026-09-23, usuario)

TDD OFF: el usuario pidió "no más TDD, todo será e2e de mi lado, no más pruebas". No escribir pruebas nuevas; las 121 existentes se conservan y solo se corren si es barato. Validación por corrida real/e2e. La subida de 89 imágenes fue única; no se planean cargas masivas de imágenes, así que no se migra a la API S3 de R2. `--sample N` añadido al CLI de R2 para corridas cortas.

## Cierre real solicitado (2026-09-23)

Reanudación autorizada: el usuario descartó Context.dev por su costo y pidió continuar la tarea anterior. No se instaló ni se llamó su API ni se consumieron créditos. Se levanta la pausa por evaluación del proveedor; continuar adquisición directa, sin reabrir ese piloto. C02 terminó: RED del autor 7/23 fallos; verificación independiente aprobó 31/31 focales y 75/75 generales, sintaxis y whitespace; el padre repitió 31/31. Se autoriza avanzar a B con los límites siguientes. Mantener A intacto y obtener B con la misma página/tamaño y alcance comprobados en su manifest, no usar a ciegas el ejemplo `--page-size 100` del autor.

Objetivo: cerrar Etapa 2 con evidencia real y verificable, sin publicar Etapa 3 ni rebajar las puertas de salida. Jalisco es el límite permanente; las cédulas pertenecen por publicación en el registro estatal, no por lugar del hecho/residencia. El usuario autorizó el trabajo necesario de Etapa 2 y eligió explícitamente TDD estricto (`strict_tdd` mediante pregunta interactiva). Las decisiones personales de la asociación y las mutaciones de cuenta no se presumen autorizadas por ese objetivo general.

TDD: ON por elección explícita del usuario. Runner: `node --test`; `npm test` no es el runner válido. Cada implementación debe demostrar RED, GREEN y REFACTOR; conservar comandos/resultados y ejecutar suite completa al cerrar la unidad. Pruebas solo sintéticas salvo harness privado expresamente delimitado. Mantener una única escritura delegada a la vez; ODD sin SDD.

- [x] S2-C01 — Comparador offline seguro de baselines. Completado: tres archivos nuevos, sin cambios al motor de reconciliación. Verificación independiente aprobada 21/21 focales y 61/61 generales; el padre repitió 21/21. Rechaza snapshots incompletos, corruptos o incompatibles antes de comparar y no muta registros ni emite identificadores/datos personales. Límite observado: fixtures construyen parte del checkpoint; integración real del productor se añade en C02.
- [x] S2-C02 — Endurecer captura antes de B: fuente Jalisco, timeout, tamaño, ritmo/reintentos y corrección de completitud/resume. Completado, verificación independiente y comprobación focal del padre aprobadas. Resume limpio soportado; corridas fallidas/legacy se conservan y requieren otro directorio, sin reparación silenciosa. Superficies: `scripts/jal-repd-ced-baseline.cjs`, `src/jal-repd-ced/baseline.cjs`, `src/jal-repd-ced/storage.cjs` solo si es necesario, nuevo `src/jal-repd-ced/http-client.cjs`, `tests/jal-repd-ced-baseline.test.cjs`, nuevo `tests/jal-repd-ced-http-client.test.cjs`, `tests/jal-repd-ced-compare.test.cjs`. Pruebas TDD sintéticas, sin red real; guardar registro de páginas completadas y reintentar fallidas sin fingir completitud. Añadir prueba productor→comparador.
- [x] S2-C03 — Segunda observación metadata-only y comparación real A/B. Completado: comparación real verificada, reportes corregidos y relectura independiente aprobada; comprobación documental del padre sin errores. No cierra por sí sola G2/G3/G4. A inmutable: `/Users/chris/Documents/seguridad-mexico/s2-full-baseline-clean-20260923T193837Z`. B nuevo: `/Users/chris/Documents/seguridad-mexico/s2-second-baseline-20260923T214140Z`. Comando: `node scripts/jal-repd-ced-baseline.cjs --output-dir /Users/chris/Documents/seguridad-mexico/s2-second-baseline-20260923T214140Z --assets skip --page-size 12 --max-pages 1000 --no-resume`. Tope de 1000 páginas y 1800 s; transporte 1 solicitud/s, 3 intentos, 20 s e 1 MiB por intento. Si el origen supera el tope o hay fallos, preservar parcial y no afirmar completitud ni reiniciar sin revisar. Comparar solo si B completo mediante CLI offline, sin cambiar A/estados/publicación. Reporte saneado en `reports/stage-2-reconciliation.md`; actualizar `reports/stage-2-status.md` sin declarar G3/G2/G4 cerrados por esta sola prueba. Registrar parámetros, tiempos, cobertura, conteos de diferencias y anomalías; ausencia no implica localización. El tope no recorta el alcance si totalPages observado es menor o igual a 1000; el comparador exige cobertura total real. Primer preflight se detuvo sin leer A ni enviar requests: padre privado tenía modo 0755. Usuario eligió explícitamente cambiarlo a 0700 y mantener ruta B. El padre aplicó y verificó chmod no recursivo solo a `/Users/chris/Documents/seguridad-mexico`, propietario intacto; reanudar C03 con los mismos límites. Resultado del worker: una captura B completa de 853/853 páginas y 10 234 registros, 21:49:02Z–22:03:17Z, sin errores ni bounds cambiantes; integridad A validada y A sin modificaciones según comprobación del autor. Intervalo A/B: 7 824.579 s. Comparador repetido por el padre: success, unchanged 10 234, demás categorías 0, anomalías ninguna. Verificador confirmó el mismo agregado y permisos B 0700/archivos 0600. Encontró correcciones documentales: cero cambios reales es válido con fixtures; la brecha 47 puede mantenerse explícitamente no conciliada; el alcance de imágenes es hasta 100; no presentar el digest histórico A antes/después como reconstruido independientemente. G2/G3/G4 siguen abiertos por requisitos operativos concretos, no por esperar artificialmente cambios reales. Corrección delegada limitada a los dos reportes, sin nueva captura.
- [x] S2-C04 — Artefactos oficiales SESNSP/INEGI/CONAPO versionados y subconjuntos Jalisco verificables. COMPLETADO 2026-09-24: SESNSP 2015-2025 importado (`s2-c04-sesnsp-2015-2025-20260924T032818Z`, Latin-1, 125/125, 14998=27,785 no especificado). AVANCE previo: CONAPO y SESNSP RNID 2026 importados por descarga manual del usuario (`reports/stage-2-c04-official-imports.md`, evidencia `s2-c04-official-import-20260924T032403Z`); CONAPO 375/375 = EXP-06. Historial: Parcial: C04a INEGI completado y verificado, incluidas las guardas de ruta. CONAPO y SESNSP bloqueados por originales oficiales inaccesibles; se pidió descarga manual al usuario. Continuar C05 mientras llegan archivos. Evidencia nueva y exclusiva: `/Users/chris/Documents/seguridad-mexico/s2-inegi-import-20260923T221320Z` (padre 0700, umask077); no tocar A/B. Adquisición serial de las dos URLs oficiales, hasta dos intentos solo por error transitorio, timeout20s por operación y deadline120s por recurso; sin redirects fuera del host ni credenciales. No rebasar límites ni inventar versión/CRS si faltan. C04 completo no se marca terminado hasta resolver cada fuente; C04a puede quedar completo por separado. C04a mapeado: nuevos `src/geo/inegi-import.cjs`, `scripts/import-inegi.cjs`, `tests/inegi-import.test.cjs`, actualización de `sources/MX-INEGI-GEO.md` y reporte `reports/stage-2-geo-import.md`. Importador offline de catálogo/geometría locales, mapeo nomgeo→name, unión exacta por cvegeo textual estatal14, exactamente125municipios sin duplicados/extras, geometrías válidas, metadatos CRS y versión oficial conservados (unknown si no declarada, nunca sustituir por observedAt). Adquirir originales solo bajo evidencia privada con hash/tamaño/fecha/URL oficiales; catálogo max1MiB, geometría max64MiB, descargas seriales acotadas sin auth ni evasiones, sin copiar grandes raws a Git. Rutas oficiales verificadas: `https://gaia.inegi.org.mx/wscatgeo/v2/mgem/14` y `https://gaia.inegi.org.mx/wscatgeo/v2/geo/mgem/14`. Adquisición real completada: catálogo 24 311 bytes y geometría 8 998 897 bytes, ambos HTTP200 sin redirect; vector declara Marco Geoestadístico diciembre2025, CRS no declarado explícitamente. Materialización real de125GeoUnits y réplica independiente con conjuntos/hash coincidentes, permisos0700/0600. Autor82/82full, padre11/11focal; verificador confirmó82/82 e integridad real pero encontró que falta rechazar salida dentro de Git y aliases de padres symlink. Corrección TDD acotada a `src/geo/inegi-import.cjs` y `tests/inegi-import.test.cjs`, sin refetch: tres regresiones RED, luego 14/14 focales y 85/85 generales según autor. Replay `materialized-path-guard/` conserva los125GeoUnits byte a byte y permisos0700/0600. Reverificación independiente aprobó las guardas: 14/14 focales, 85/85 generales, sintaxis/diff limpios, artefactos125GeoUnits idénticos y privados. C04a aceptado; no equivale a cerrar C04 completo ni G2/G3/G4. CONAPO: enlace oficial del PDF redirigió por HEAD301 a `https://www.gob.mx/conapo/work/models/CONAPO/pry23/DBMun/14_Jalisco.zip`, que respondió404. URL histórica exacta del CSV de datos.gob.mx también respondió HEAD403 actual, sin redirección; no continuar solicitudes por esa ruta ni eludir controles. EXP-06 conserva derivados oficiales históricos de Jalisco (750 filas sexo/año/municipio, 375 totales, años2020/2025/2026, unión125municipios), pero no original completo ni hash original. Los derivados no retienen grupos de edad; no sustituyen adquisición completa. Recurso oficial `3c3092be-583e-4490-8c23-67ef9a64b198`, archivo `pobproy_quinq1.csv`, tamaño histórico36 658 654bytes; solicitar descarga manual oficial y mantener procedencia histórica explícita. TDD: `node --test tests/inegi-import.test.cjs tests/geo-canonical.test.cjs`, luego `node --test tests/*.test.cjs`; harness posterior con artefactos oficiales reales privados. No sustituir datos reales por fixtures ni añadir dependencias sin necesidad demostrada. Superficie adicional: `reports/stage-2-status.md` para avance saneado de INEGI sin cerrar las demás fuentes. C04a prevé 350–550 líneas, unidad coherente con pruebas/contrato; sin commits. Rollback únicamente código/documentación nuevos o cambios de C04a, nunca borrar evidencia ni revertir trabajo anterior.
- [x] S2-C05 — Subconjunto de hasta 100 imágenes, validación/cuarentena y almacenamiento privado R2. COMPLETADO 2026-09-23: ledger `s2-assets-r2-89-v1` 89/89 verified, 8 250 929 bytes, sin lock; muestra real `--sample 5` y replay ok (5 verified, 0 PUT, 13.2s/10.1s). Historial: C05a: actualizados solo `SPEC_JALISCO_STAGE_2.md`, `SPEC_JALISCO_DATA_FIRST.md` y `sources/JAL-REPD-CED.md`; revisión independiente aprobada. Cobertura metadata completa separada del subconjunto determinista ≤100; unidad documental sin RED/GREEN, con diffcheck/relectura. C05b después: selector/descarga/validación/cuarentena local con TDD; C05c: adaptador R2 idempotente y comprobación lectura/hash, TDD y evidencia real. Antes de descargar, perfilar privadamente orígenes/formato/tamaño sin exponer datos personales; antes de subir, comprobar exposición/lifecycle efectivos de `seguridad-jalisco-private` en lectura, no inferir privacidad del binding PRIVATE_ASSETS. Sin tocar PUBLIC_DERIVATIVES ni desplegar. Preflight real 2026-09-23T22:40Z en `/Users/chris/Documents/seguridad-mexico/s2-assets-preflight-ok92nbey`: r2.dev desactivado, cero dominios personalizados y solo aborto de multipart incompleto a7d (no expiración de objetos almacenados). B contiene10228referencias HTTPS al origen `https://repd.jalisco.gob.mx`,6ausentes; tres HEAD deterministas devolvieron200 image/jpeg y tamaños declarados18 523–167 772bytes, sin leer imágenes. C05b acotado: fuente B inmutable, máximo100candidatos congelados,2MiB/cuerpo,64MiB total consumido contabilizando intentos/cuarentena,serial1req/s,timeout20s,cota600s,2intentos máximo por candidato solo transitorios; detener auth/429, sin redirects. Output privado nuevo `/Users/chris/Documents/seguridad-mexico/s2-assets-subset-100-v1`; no subir R2 en C05b. Superficies C05b: `src/jal-repd-ced/assets.cjs`, `src/jal-repd-ced/asset-http-client.cjs`, `scripts/jal-repd-ced-assets.cjs`, `tests/jal-repd-ced-assets.test.cjs`, `reports/stage-2-assets.md`; permitir únicamente exportar `readSnapshot` existente en `src/jal-repd-ced/compare-baselines.cjs` para reutilizar validación, sin alterar comparador. Si evita duplicar guardas, extraer únicamente la protección de salida ya verificada a `src/private-output.cjs` y hacer que `src/geo/inegi-import.cjs` la reutilice sin cambiar semántica/serialización; incluir regresiones INEGI en la verificación. TDD estricto focal/general, prueba real una vez después de GREEN y replay local sin red; previsión500–800líneas coherentes incluyendo pruebas. C05b ejecutado una vez:100candidatos,89validados,11cuarentenas solo motivo invalid_image,0fallidos/pendientes;100intentos,10 111 848bytes,99.499s. Autor10/10focal,25/25regresiones,95/95full; padre repitió10/10. Replay local verify-only y revisión independiente aprobados:95/95pruebas,89archivos/8 250 929bytes almacenados sin discrepancias de hash. Padre confirmó89hashes distintos, tamaños4 290–1 324 923bytes. No más descargas ni reemplazos de cuarentena; invalid_image es fallo de política firma/marcadores, no prueba de corrupción. Validación limitada a MIME/firma/marcadores, no decodificación de píxeles. C05c parcial, aún sin objetos remotos. Primer intento: autor14/14focal,47/47regresiones y109/109full; preflight real devolvió unsafe_bucket (exit2), PUT0/GETdeobjetos0. Ledger raíz existe0700 con solo wrangler.log0600, sin manifest/state. Diagnóstico independiente confirmó rechazo por formato: igualdad del stdout completo no tolera banner/progreso conocidos de Wrangler; log compatible con política privada, sin nueva comprobación remota aún. Corrección acotada a `src/jal-repd-ced/wrangler-r2.cjs` y `tests/jal-repd-ced-private-r2.test.cjs`, RED/GREEN con formato realista y casos inseguros/desconocidos rechazados. Corrección completada por autor: parser RED14/15→15/15; raíz RED3fallos/18→18/18; suite113/113 y sintaxis/whitespace limpios. Preflight fresco final exit0 private_no_expiration confirmó pública deshabilitada,0dominios y solo abort multipart7d. Raíz conserva log+cache privados, sin manifest/state ni GET/PUT de objetos. Auditoría independiente C05c pidió corrección:110→113pruebas pasan pero faltan comprobaciones de deadline tras awaits/antes de PUT/éxito; un GET tardío puede disparar PUT y preflight final tardío devolver ok. No subir. Corrección TDD: `src/jal-repd-ced/private-r2-upload.cjs`, `src/jal-repd-ced/wrangler-r2.cjs`, `tests/jal-repd-ced-private-r2.test.cjs` y actualización histórica `reports/stage-2-assets.md`; comprobar presupuesto restante también en procesos reales sin ampliar30s/900s. Corrección de plazo terminada por autor: RED18/23→GREEN23/23, suite118/118; presupuesto absoluto propagado a cada proceso, preservando bytes/put_inflight. Padre repitió23/23. Reporte actualizado con nota de autor2026-09-24UTC; sin repetir preflight ni operaciones remotas. Reverificación independiente aprobada:23/23focal y118/118full, sintaxis y whitespace explícito de archivos untracked limpios; hueco de plazo resuelto. Se autoriza ahora ejecución delegada del alcance ya consentido: preflight fresco, una corrida sobre89objetos y replay remoto0PUT en el mismo ledger, sin cambios de código. Intento real posterior: preflight fresco exit0 en3s; primera ejecución inicial exit2 remote_uncertain en4s al primer GET, sin PUT ni replay. Ledger ahora inicializado:89pending,0put_inflight/verified,8 250 929bytes locales, sin lock residual y privado. Diagnóstico independiente confirmó marcador de clave inexistente con exit1/stdout1byte whitespace/stderr190; el adaptador exigía stdout0. HTTP404 no observado. Dos invocaciones GET hasta diagnóstico,0PUT. Corrección TDD completada por autor: RED23/26→GREEN26/26 y121/121full; formato de ausencia estricto y control/payload separados sin ocultar consumo. Una sonda real del primer descriptor devolvió missing,consumed1,controlBytes1,bytesobjeto0. Total histórico3invocaciones GET,0PUT; ledger89pending,0inflight/verified intacto. Reverificación independiente confirmó contadores y demás guardas, pero pidió restringir stdout de ausencia al LF observado (conservar compatibilidad vacía), no TAB/CR/SPACE. Corrección mínima LF completada solo en adapter/tests: RED25/26→GREEN26/26, suite121/121; sin red ni cambios de estado. Reverificación final independiente aprobada (26/26focal,121/121full). Runtime completado por autor: --resume exit0 en254s,89PUT/178GET,89objetos y8 250 929bytes verificados por lectura remota; replay exit0 en86s,0PUT/89GET,89coincidencias. Postread89verified,0pending/inflight, binding/permisos correctos y sinlock. Conteos son invocaciones Wrangler, no HTTP/facturación. Solo falta aceptación independiente de esta evidencia para cerrar C05c; no más correcciones conocidas ni otra subida necesaria. Usuario pidió resumen por demora: responder estado/causas, no iniciar nuevos trabajos en esa consulta. Preservar manifest/state/log/cache. Próxima corrida deberá usar --resume después del diagnóstico/corrección/verificación, nunca inicializar otra vez ni ampliar límites. Solo preflight de lectura en esta corrección; auditar C05c completo antes de autorizar su ejecución de subida delegada. Precheck posterior encontró también `.wrangler/`0700 generado por proveedor; además CLI --preflight exigía siempre destino nuevo. Conservar ambos artefactos. Corrección ampliada y aplicada solo a `scripts/jal-repd-ced-private-r2.cjs` y `src/jal-repd-ced/private-r2-upload.cjs`: aceptar misma raíz preflight validada y cache de proveedor estructuralmente reconocido, privado y sin symlinks, no directorios/archivos arbitrarios; probar nueva raíz y preflight repetido y reanudación de ledger. No borrar/reiniciar ni cambiar permisos; sigue prohibido GET/PUT de objetos hasta revisión independiente. Adaptador privado con cliente inyectado, exportación mínima de descriptores ya validados, GET antes de PUT y lectura/hash remotos, ledger vinculado al lote;89objetos únicos previstos, cuarentena excluida. Superficies: `src/jal-repd-ced/assets.cjs` (solo exportación de inspección validada), `src/jal-repd-ced/private-r2-upload.cjs`, `src/jal-repd-ced/wrangler-r2.cjs`, `scripts/jal-repd-ced-private-r2.cjs`, `tests/jal-repd-ced-private-r2.test.cjs`, `reports/stage-2-assets.md`. Ledger/evidencia NUEVOS `/Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1`, fuera del lote local C05b inmutable. Bucket fijo privado existente, sin cambios de permisos/Worker; comprobaciones frescas de exposición/lifecycle antes de escribir y al terminar. Máximo2MiB por objeto/lectura,64MiB de payload por ejecución,30s por proceso y900s de corrida; ante GET incierto o distinto, detener sin sobrescribir. Tests RED/GREEN antes de escrituras remotas; corrida real y replay deben verificar89objetos, replay sin PUT, no borrar evidencia. Wrangler --pipe remoto confirmado binario en código instalado4.136.1: sin banner/progreso y pipeline del cuerpo a stdout; ausencia remota produce error específico, no tratar cualquier fallo como ausencia. Previsión500–800líneas con pruebas, sin commits. En Wrangler confinar stdout/stderr y configurar WRANGLER_LOG_PATH como archivo privado explícito terminado en .log (evita limpieza automática de logs), sanitización activa y métricas desactivadas; relectura remota acotada, nunca evidencia solo local. El usuario sustituyó el requisito de todas las imágenes por unas 100 para collage, manteniendo datos lo más completos posible; actualizar especificación/contratos antes del pipeline y declarar ambas coberturas por separado. Autorizó escritura en buckets privados existentes sin cambiar permisos; acceso solo suyo/automatizaciones autorizadas, revisión de conservación en 30 días, sin publicación ni borrados automáticos. No eligió los presupuestos propuestos de 5/15 GiB: definir límites técnicos conservadores para hasta 100 imágenes y pausar si no bastan.
- [x] S2-G3 — Demostrar mantenimiento durable y tratamiento de frescura. COMPLETADO 2026-09-24 (e2e, sin TDD): ledger de supresiones persistente `src/jal-repd-ced/suppressions.cjs` + CLI, `--suppressions` en compare, sonda `scripts/jal-repd-ced-freshness.cjs`; evidencia en `reports/stage-2-g3-maintenance.md`. Origen sin ETag/Last-Modified/Cache-Control. Historial: Pendiente tras C05c; no repetir captura completa por cero diferencias. Unidad mínima propuesta: ledger privado de exclusiones persistido, integración offline con snapshots validados y pruebas sintéticas write→reload→reimport→backup/restore; restauración deliberada auditada separada. No fingir retiros reales ni modificar A/B. Añadir sondeo acotado con procedencia de headers/caché, tiempos y estado; Date o ausencia de validadores no prueban frescura del origen. Clasificar desconocido y escalar, sin afirmaciones retrospectivas sobre headers A/B no retenidos. Receta operativa con cadencia propuesta (no longitudinalmente probada), topes y métricas; superficies exactas se delimitarán antes del writer.
- [x] S2-C06 — Resolver/documentar REPD-STATS y ejecutar benchmark/ADR con métricas reales. COMPLETADO 2026-09-24 (e2e): `reports/stage-2-c06-benchmark-adr.md`. REPD-STATS: 16,203 del mapa = 16,117 municipales + 86 SE IGNORA (modelo corregido); brecha 47 = 38H/9M fuera de toda clave del mapa, sigue sin explicar. D1 `seguridad-jalisco-bench-d1` creada, sintética: 235,483 lecturas/30,507 escrituras/1.5MiB; motor p95 ≤4.4ms. ADR propuesta: D1, sin Postgres. Historial: Pendiente; no elegir datastore por expansión territorial ni inventar resultados. D1 medido obligatorio para ADR; Postgres/PostGIS solo si GIS pesado o estrés D1 justificados en Jalisco. No hay bindings D1/PG en config (no demuestra inventario remoto). AUTORIZADO por elección explícita del usuario `authorize-isolated-d1-benchmark-bounded`: crear UNA D1 aislada de benchmark, solo datos sintéticos/agregados a cardinalidad observada; nada personal, sin deploy, sin cambiar plan. Topes:1Mfilas leídas,50kfilas escritas incluyendo índices/DDL,64MiB almacenados; revisar conservación30d sin borrado automático. Tarifas oficiales consultadas:https://developers.cloudflare.com/d1/platform/pricing/ ($0.001/Mlecturas,$1/Mescrituras,$0.75/GB-mes fuera de inclusiones); estimación marginal≈US$0.11primer mes si todo facturable, no tope de factura de cuenta. Creación y benchmark autorizados solo dentro de esos topes; todavía no ejecutados. No consultar/escribir bases ajenas, crear otra D1, subir plan ni provisionar Postgres. Delimitar superficies y harness antes del writer, tras C05c/G3; conservar consumo acumulado entre ejecuciones y detenerse ante cupo o gasto incierto. Medir motor/servicio y tiempo cliente por separado, no afirmar SLA de aplicación Stage3 inexistente ni llamar D1 a SQLite local.
- [x] S2-C07 — Verificación integral, políticas de asociación y reporte de cierre G2/G3/G4. COMPLETADO 2026-09-24: el usuario ES la asociación y dueño de todos los roles (decisión permanente, no volver a preguntar). Políticas y ADR aceptada en `reports/stage-2-c07-closure.md`. ETAPA 2 CERRADA. Pendiente; no marcar cierre mientras falten criterios obligatorios, autorizaciones o responsables.

Aceptación C01: directorios distintos, manifiesto/checkpoint/NDJSON consistentes; fuente/endpoint de Jalisco compatibles; fechas válidas y sucesivas; scope completo, assets `skip`, rangos/páginas y bounds finales estables, contadores y hashes íntegros, cero fallos/errores/duplicados. Reportar únicamente estado/conteos saneados. Ausencia no significa localización y no se infiere cambio de bytes de assets sin haberlos observado.

Verificación C01: `node --test tests/jal-repd-ced-compare.test.cjs tests/jal-repd-ced-reconcile.test.cjs`, después `node --test tests/*.test.cjs`. Harness: invocación del CLI offline exclusivamente con fixtures sintéticos privados temporales desde pruebas; sin red ni datos reales en esta unidad. Evaluación de riesgo y verificación independiente después del writer, sin inventar cierre nativo.

Entrega: sin commits/push/PR/deploy. Estrategia `ask-on-risk`; C01 prevista en aproximadamente 300–400 líneas añadidas/eliminadas, límite orientativo, no omitir pruebas por tamaño. El cierre completo excederá una unidad; trabajar secuencialmente por comportamiento, sin agrupar cambios previos del usuario. Si se pide entrega en Git, resolver partición antes del primer commit. Rollback C01: retirar solo archivos nuevos de C01 y revertir únicamente sus cambios puntuales; jamás resetear el árbol existente.

Evidencia de reanudación: la auditoría identificó que `reconcileCedulas` no valida manifiestos/checkpoints y el CLI de captura admite endpoints arbitrarios sin límites de red suficientes. La referencia A es `/Users/chris/Documents/seguridad-mexico/s2-full-baseline-clean-20260923T193837Z` (853 páginas, 10 234 registros según reporte histórico); B todavía no ejecutado. No se encontró espejo completo antiguo en Engram; este documento existente se conserva como autoridad y se establece espejo actualizado. C01 implementado en tres archivos nuevos (435 líneas; sobre la previsión orientativa para incluir validaciones y pruebas). Autor observó RED por módulo ausente, GREEN 21/21 y suite completa 61/61; no modificó reconciliación. C01 quedó inicialmente pendiente de verificación independiente (aprobada después, como se registra abajo). INSPECT nativo se detuvo solicitando selección de archivos no rastreados; no se creó lineage ni cierre nativo. El candidato ambiente mezcla cambios previos y dependencias no rastreadas; no se congeló como si fuera exclusivamente C01. ASSESS no evaluable exige verificador independiente. Resultado posterior: gentle-ai-verify aprobó C01 y el padre confirmó 21/21. Próximo paso: C02, sin capturar B aún. C02 mantendrá el contrato de snapshots existente y validará endpoints antes de red/escrituras; valores conservadores previstos: una petición a la vez, intervalo mínimo de 1 s, timeout de 20 s por intento incluyendo cuerpo, hasta 1 MiB por respuesta y 3 intentos como máximo, sin seguir redirects ni reintentar autenticación. Verificación C02: `node --test tests/jal-repd-ced-http-client.test.cjs tests/jal-repd-ced-baseline.test.cjs tests/jal-repd-ced-compare.test.cjs`, después `node --test tests/*.test.cjs`; harness con fetch inyectado y snapshots temporales sintéticos. Pronóstico 400–650 líneas, aceptado como unidad coherente de seguridad de captura, sin dividir artificialmente ni hacer commits. Rollback solo de los cambios C02, preservando C01 y el trabajo previo. Investigación federal delegada sin herramientas web disponibles: sus rutas son evidencia del repositorio, no enlaces actuales verificados; el padre dispone de herramientas web para continuar.

Operating constraints:
- Continue ODD; do not initialize SDD.
- Use one fresh Orca/Pi worker terminal per substantial todo and release it after completion.
- Do not commit, push, publish, deploy, or create PR unless explicitly authorized.
- Do not put raw personal data, names, photos, exact media URLs, raw HAR, secrets, or downloaded assets in Git.
- Keep private/raw evidence under `/Users/chris/Documents/seguridad-mexico`.
- Treat observed REPD endpoints as implementation endpoints, not official APIs.
- Stop or mark blocked for credentials, secret-bearing commands, Cloudflare account mutations, destructive operations, publication decisions, or irreversible privacy decisions.

Stage 2 interpretation:
- Fase 0 Cloudflare actions require credentials/account authority. This session may probe local Wrangler/auth state and prepare repo-safe scaffolding, but must not create buckets/widgets/secrets/deployments without explicit credentialed authorization.
- Full `JAL-REPD-CED` baseline can be prepared and piloted. Complete live metadata traversal and especially full asset acquisition must be bounded and must write only to private evidence storage or approved R2 private storage.
- Assets cannot be stored in Git. If R2 is not available, asset acquisition remains blocked or local-private-only by explicit run policy; local storage alone does not prove required R2 writing. Updated user scope: up to 100 images, not all images; preserve full metadata scope and do not describe that subset as integral asset coverage.

Historical implementation queue (checked items describe delivered scaffolds/reports, not acceptance of Stage 2 gates):
- [x] S2-000 Analyze Stage 2 spec and produce executable task graph.
- [x] S2-001 Probe Cloudflare/Wrangler readiness and prepare non-secret bootstrap artifacts; block real account mutations if credentials/authorization are absent.
- [x] S2-002 Create sanitized source contracts for Stage 2 sources.
- [x] S2-003 Implement local/private storage adapter interfaces for snapshots/manifests/quarantine; R2 adapter scaffold only if safe without secrets.
- [x] S2-004 Implement baseline runner for `JAL-REPD-CED` metadata with checkpoints, resume, hashes, manifest and private output.
- [x] S2-005 Run bounded baseline pilot and decide whether complete metadata baseline is safe to run now.
- [x] S2-006 Implement canonical model/provenance and reconciliation engine with synthetic maintenance suite.
- [x] S2-007 Resolve/model REPD-STATS discrepancy or preserve unreconciled definitions with evidence.
- [x] S2-008 Establish SESNSP acquisition path: official automated or manual official versioned import contract.
- [x] S2-009 Materialize INEGI/CONAPO canonical layer from private evidence/contracts.
- [x] S2-010 Cloudflare-first ADR and D1 vs Postgres benchmark plan based on measured data; do not over-decide.
- [x] S2-011 Consolidate Stage 2 status report, gates G2/G3/G4, blockers, tests, and next steps.

Evidence log:
- S2-000: Stage 2 graph separates repo/local tasks, Cloudflare credential/mutation blockers, policy blockers, and live-source-budget tasks.
- S2-001: `reports/stage-2-cloudflare-readiness.md` — Wrangler not installed locally/globally, Cloudflare account/token vars absent, `.env.example` created with names only, real Cloudflare mutations blocked pending authorization.
- S2-002: `sources/` — five sanitized Stage 2 source contracts created for JAL-REPD-CED, JAL-REPD-STATS, MX-SESNSP, MX-INEGI-GEO, and MX-CONAPO-POP.
- S2-003/S2-004: `src/jal-repd-ced/storage.cjs`, `src/jal-repd-ced/baseline.cjs`, `scripts/jal-repd-ced-baseline.cjs`, `tests/jal-repd-ced-baseline.test.cjs` — local-private storage and resumable metadata baseline runner implemented; default endpoint corrected to observed JSON API; tests pass 14/14 with offline connector suite.
- S2-005: `/Users/chris/Documents/seguridad-mexico/s2-005-baseline-pilot-20260922T014554Z/access-report.md` — 3-page metadata-only pilot succeeded with 36 unique records, 0 failures, 0 duplicates, 0 schema errors, unchanged bounds.
- S2 full metadata baseline: first full attempt `/Users/chris/Documents/seguridad-mexico/s2-full-baseline-20260923T192749Z/access-report.md` traversed all pages but remained partial because schema nullability was too strict for `colonia`, `estatura`, and `ruta_foto`; clean rerun `/Users/chris/Documents/seguridad-mexico/s2-full-baseline-clean-20260923T193837Z/access-report.md` completed 853/853 pages with 10,234 unique records, 0 failures, 0 schema errors, 0 duplicates, and unchanged bounds.
- S2-006: `src/jal-repd-ced/canonical.cjs`, `src/jal-repd-ced/reconcile.cjs`, `tests/jal-repd-ced-reconcile.test.cjs` — DB-independent canonical/provenance and reconciliation suite implemented; tests pass 26/26 with baseline/offline tests.
- S2-007/S2-008/S2-009: `reports/stage-2-sources-resolution.md` plus `src/jal-repd-stats/`, `src/sesnsp/`, `src/geo/` — REPD-STATS gap modeled as unreconciled 47, SESNSP manual official import scaffold created, INEGI/CONAPO canonical/rate helpers created; tests pass 37/37 across current suites.
- S2-010: `reports/stage-2-architecture-adr-plan.md` — Cloudflare-first supported decisions documented, credential/resource blockers identified, and D1 vs Postgres/PostGIS benchmark plan defined without forcing the datastore ADR.
- S2-011: `reports/stage-2-status.md` — consolidated Stage 2 status, S2-001..S2-018 table, G2/G3/G4/G5 gate status, explicit blockers, evidence index, ordered next actions, and current full test result (`node --test tests/*.test.cjs`, 52/52 passing after schema nullability and Worker tests).


- S2-001 staging validation update: Cloudflare staging deployment is now validated by safe HTTP checks: public `/health` returns `200` with staging environment, public `/admin/health` returns `403`, public missing route returns `404`, and admin hostname `/health` returns `302` to Cloudflare Access without following redirects. User-reported operational facts preserved: staging deploy succeeded, `TURNSTILE_SECRET_KEY` is stored for staging, DNS is correct for staging/admin hostnames, Access app `71cbb3d9-ce33-4eea-b6e5-0e98761db5f1` protects the admin hostname, observability is active, and `CF_DNS_API_TOKEN` covers DNS + Zero Trust without recording token values.

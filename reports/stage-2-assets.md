# Stage 2 C05b — local private image subset

Status: local acquisition completed; **not R2 storage, readback, publication, or full image coverage**. Private evidence: `/Users/chris/Documents/seguridad-mexico/s2-assets-subset-100-v1` (new directory; no original image bytes or references in Git). The complete, independently validated metadata-only B snapshot was read without mutation. No changes to its `assetPolicy`.

## Execution

- Command (foreground, externally bounded to 660 seconds): `node scripts/jal-repd-ced-assets.cjs --baseline-dir /Users/chris/Documents/seguridad-mexico/s2-second-baseline-20260923T214140Z --output-dir /Users/chris/Documents/seguridad-mexico/s2-assets-subset-100-v1 --limit 100`. Wall time 99.499 s, exit 0, no stderr. One initial capture only.
- Full metadata denominator: **10,234** records, **10,228** nonempty photo references and **6** missing references. The frozen textual `(source_id, id_cedula_busqueda)` selection took **100** references; **10,128** usable references were not selected. This subset is neither representative nor complete asset coverage.
- Selected-image denominator: **89** locally signature-validated, **11** reason-only quarantined (`invalid_image`), **0** failed, **0** pending. **100** requests/attempts; **10,111,848** consumed body bytes; no stop code. Quarantined references remain private, not published or replaced by later candidates. HTTP HEAD and `.jpg` hints were not treated as body validation.
- Offline `--resume --verify-only` readback independently rechecked baseline/selection, cached byte SHA-256, MIME/signatures, private modes and state ledger: same aggregates, 0.264 s, exit 0. Completed `--resume` returned the same aggregates in 0.259 s, exit 0 and **zero network requests** (the completed path returns before the fetch loop; synthetic injected-fetch regression guards this). Private root/images directories are 0700 and state/manifest files 0600; cached objects are checked as 0600 on readback. Parent remained canonical, owned by the current user and 0700.

The selector freezes the validated baseline's five evidence files digest, observation/version, textual selected identities and exact private references. Outcome states are mutually exclusive; requests reserve their body budget before each attempt and preserve attempts/bytes across a safe resume. Unfinished in-flight state or unexpected files fail closed. Limits: up to 100 selected references, 2 MiB per response, 64 MiB accounted body bytes, one request at a time with at least 1 s between starts, 20 s fetch-and-body timeout, 600 s run deadline, at most two attempts per candidate for transient network/5xx only. Authentication/429 stops the cohort, and redirects and other hosts are rejected. A transport that supplies an oversized single chunk can cross a remaining byte allowance before software can stop it; the actual consumed chunk is recorded and the cohort stops rather than understating bytes. This did not occur in the live run.

Assurance is **MIME plus bounded header/signature/terminal-marker validation**, not full pixel decoding, CRC/decompression, crop, collage, or content moderation. PNG header dimensions are bounded to 40 million pixels; this is not a proof that the image decodes or that its visual content is appropriate. SHA-256 and per-reference provenance are private; this report intentionally contains no per-image hashes, keys, IDs, names or media URLs. Retention review is due at 30 days, without automatic deletion. Access remains owner/authorized automations only.

## C05c — private R2 upload attempt (partial; no objects uploaded)

An injected-client adapter and bounded installed-Wrangler process adapter were implemented with frozen private cohort/descriptor manifest, opaque cohort-bound keys, GET-before-PUT, persisted `put_inflight` transitions, mandatory post-PUT GET/SHA-256, and GET-only replay. Only the fixed `seguridad-jalisco-private` bucket is accepted by the CLI. The CLI checks for exactly 89 validated descriptors totaling 8,250,929 local bytes before any remote request; 11 `invalid_image` reason-only quarantines remain excluded (a marker-policy failure, **not** proven image corruption). No original B, source mappings, or quarantined bytes were uploaded.

Synthetic RED: missing upload module, focused test command failed 0/1. GREEN: `node --test tests/jal-repd-ced-private-r2.test.cjs` passed 14/14; C05b/INEGI/comparator/reconciliation regression command passed 47/47; full `node --test tests/*.test.cjs` passed 109/109. Syntax and scoped whitespace checks passed. Synthetic tests do not prove live remote behavior.

After GREEN, a fresh read-only effective-bucket preflight was attempted through installed Wrangler with a bounded process, explicit sanitized private `.log`, metrics disabled, and no raw control output exposed. It returned the stable `unsafe_bucket` refusal. The private runtime root `/Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1` was absent before this attempt and now contains only its owner-private 0600 Wrangler log (directory 0700). The failure could indicate changed bucket policy **or** an unrecognized lifecycle output shape; neither is accepted as safe. **Stop:** actual runner and GET/PUT/replay were not invoked; verified remote objects 0, R2 PUT invocations 0, remote hash/readback evidence none. No initial manifest/state was created. Do not interpret the old 22:40 preflight as current authorization or restart in another directory. Private log is retained, not published or cleared.

The implementation caps each local/remote body at 2 MiB, aggregate accounted payload at 64 MiB per execution, each subprocess at 30 s, and the run at 900 s. B and C05b remain read-only. The report does not assert independent acceptance, native review approval, or G2/G3/G4 closure. C04 CONAPO/SESNSP originals and G3 operations/G4 remain pending; retention requires review at 30 days, never automatic deletion. No 5/15 GiB budget or zero-cost claim was authorized.

### C05c author correction evidence — written 2026-09-24 UTC

The preceding `unsafe_bucket` and `output_exists` refusals describe **historical parser/root-handling defects**, not evidence of current public exposure. After their bounded correction, a new read-only `--preflight` returned `private_no_expiration` (exit 0) for the fixed bucket: r2.dev disabled, zero custom domains, only the default incomplete-multipart abort after 7 days. Its exact execution timestamp was not retained; this paragraph's written date is **not** the preflight timestamp. The existing private root contains its 0600 `wrangler.log` and the recognized owner-private `.wrangler/cache/wrangler-account.json` provider cache; no manifest/state, object GET, PUT, upload, or remote hash evidence was created.

A subsequent independent audit identified a whole-run deadline gap: a GET could finish after 900 seconds and still precede a PUT, and a late final preflight could yield success. The author added synthetic-clock regressions and bounded checks after awaited operations, before PUT and before success; Wrangler now recalculates remaining time before each control/object subprocess, never granting another 30 seconds past the run deadline. Focused `node --test tests/jal-repd-ced-private-r2.test.cjs`: RED 18/23 (five new failures), GREEN 23/23; full `node --test tests/*.test.cjs`: 118/118 passed. This is **author evidence only**, not independent re-verification or R2 storage acceptance. The actual 89-object run and replay remained prohibited pending the parent's independent review at the time of this note.

### C05c bounded runtime attempt — author evidence written 2026-09-24 UTC

After the independent deadline correction was approved, the parent authorized a single initial remote execution and one replay **only if** the initial run fully succeeded. A safe metadata-only precheck found the existing root 0700 with exactly `wrangler.log` 0600 and the recognized private `.wrangler/cache/wrangler-account.json` provider cache; no manifest, state, or lock. No cache payload was read.

- Fresh control-plane command: `node scripts/jal-repd-ced-private-r2.cjs --baseline-dir /Users/chris/Documents/seguridad-mexico/s2-second-baseline-20260923T214140Z --output-dir /Users/chris/Documents/seguridad-mexico/s2-assets-subset-100-v1 --ledger-dir /Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1 --preflight`; exit 0, `private_no_expiration`, 3 s (shell-second resolution). This is the current preflight, separate from earlier historical refusals.
- Single initial command: `node scripts/jal-repd-ced-private-r2.cjs --baseline-dir /Users/chris/Documents/seguridad-mexico/s2-second-baseline-20260923T214140Z --output-dir /Users/chris/Documents/seguridad-mexico/s2-assets-subset-100-v1 --ledger-dir /Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1`; exit 2, fixed code `remote_uncertain`, 4 s (shell-second resolution). Foreground outer timeout 960 s; internal 900 s, per-command 30 s, per-body 2 MiB, execution payload 64 MiB. The runner performed its internal fresh bucket preflight before the first object GET.
- Private metadata-only postread: exact fixed bucket, **89** frozen validated descriptors totaling **8,250,929** local bytes; ledger phases **89 pending, 0 put_inflight, 0 verified**. Manifest and state files are 0600, root 0700; lock absent. The first GET invocation was uncertain, before any `put_inflight` transition: **0 PUT invocations, 0 verified remote objects, 0 confirmed remote hash matches**. Actual underlying HTTP request count and consumed GET bytes are unknown; a Wrangler invocation is not an HTTP count. The required independent post-PUT readback did not occur. No image source was fetched. Do not infer remote absence from this error.

**Stop:** no replay was attempted because initial execution failed. Do not retry PUT, create a fresh directory, remove evidence, or infer that R2 storage succeeded. The private evidence locator remains `/Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1`; detailed errors remain private. The cause of `remote_uncertain` requires a separately authorized diagnosis, not an automatic relaxation of missing-object detection. No zero-billing claim; C04 CONAPO/SESNSP originals, G2/G3/G4 and independent remote acceptance remain open.

### C05c first-GET absence classification — author evidence written 2026-09-24 UTC

A separate independent diagnosis confirmed the first frozen descriptor's installed-Wrangler GET returned process exit 1, **one whitespace stdout control byte**, 190 stderr bytes with Wrangler's exact missing-key marker and no auth/rate markers. Installed Wrangler 4.136.1 emits this UserError when its remote object lookup returns null; **no HTTP status was observed**. The prior adapter incorrectly required zero stdout bytes and therefore returned `remote_uncertain`. This diagnostic did not establish that any other descriptor is absent.

The adapter now accepts only the installed bounded missing-key error rendering with expected exit 1 and exactly zero or one recognized ASCII whitespace control byte; its exact explicit private log path must match. It rejects extra stderr, non-whitespace/long/binary stdout, timeouts, oversize, generic errors and conflicting markers. A successful GET still preserves binary response bytes without trimming. The core accounts all consumed stdout bytes against the 64 MiB execution cap, separately subtracting the explicitly classified control byte from object-read bytes; malformed control counters fail closed. No HTTP status or underlying request count is inferred.

Strict TDD: focused `node --test tests/jal-repd-ced-private-r2.test.cjs` RED 23/26 (three new failures), GREEN 26/26; full `node --test tests/*.test.cjs` 121/121. A **single authorized read-only adapter GET probe** of the first key, privately derived from the existing manifest, then returned `{status: missing, consumed: 1, controlBytes: 1, objectBytes: 0}`. Only this first descriptor was probed. At the 2026-09-24T02:35:29Z private postread, the root and files retained private modes (0700/0600), four recognized root entries and no lock; state remained **89 pending, 0 put_inflight, 0 verified**. Across the original attempt, independent incident diagnosis, and this one probe: **three Wrangler object GET invocations, zero PUT invocations**, zero verified R2 bytes. These counts are CLI invocations, **not** measured underlying HTTP requests. No upload, replay, preflight control command, or new private evidence file was created in this correction. Parent independent re-review remained required before authorizing any `--resume` operation; G2/G3/G4 and C04 CONAPO/SESNSP remain open.

### C05c bounded R2 runtime — author evidence written 2026-09-24 UTC

After independent approval of the exact-LF correction, the parent authorized two bounded `--resume` executions against the **same existing private ledger**, with the second conditional on full success of the first. Before execution, a private aggregate-only precheck found the fixed bucket and manifest binding, 89 unique validated descriptors totaling 8,250,929 local bytes, 89 pending/0 in-flight/0 verified, only the four recognized root entries, private modes and no lock. B and the C05b local image subset remained read-only; no source fetch, quarantine replacement or full metadata upload occurred.

Both foreground runs used exactly: `node scripts/jal-repd-ced-private-r2.cjs --baseline-dir /Users/chris/Documents/seguridad-mexico/s2-second-baseline-20260923T214140Z --output-dir /Users/chris/Documents/seguridad-mexico/s2-assets-subset-100-v1 --ledger-dir /Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1 --resume`. Outer timeout was 960 s per invocation; internal whole-run cap 900 s, process cap 30 s, body cap 2 MiB and accounted execution payload cap 64 MiB. The core performed its own fresh read-only bucket preflight before and after object operations in **each** run (two preflights per run), with no bucket policy or public-access mutation.

| Run (UTC) | Exit | Wrangler object GET invocations | PUT invocations | Verified objects / bytes | Reused | Object-read bytes | Accounted payload bytes |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| First, 02:42:49–02:47:03 (254 s) | 0 | 178 | 89 | 89 / 8,250,929 | 0 | 8,250,929 | 33,003,805 |
| GET-only replay, 02:47:09–02:48:35 (86 s) | 0 | 89 | **0** | 89 / 8,250,929 | 89 / 8,250,929 bytes | 8,250,929 | 16,501,858 |

The initial 89 PUT invocations transmitted 8,250,929 verified local bytes. Each was preceded by a GET classified as missing under the exact installed CLI semantics, then followed by a **fresh remote GET whose size/SHA-256 matched**; the replay independently re-read all 89 via GET without any new PUT. The prior three diagnostic Wrangler GET invocations are historical and **not included** in the table; total observed CLI object GET invocations across that history and these two runs are 270. Underlying HTTP request counts, billing and complete pixel decoding are **not** established. The 11 `invalid_image` quarantines remain marker-policy failures, not proven corrupt images.

Private aggregate postread of `/Users/chris/Documents/seguridad-mexico/s2-assets-r2-89-v1` confirmed manifest/state binding, 89 distinct descriptor hashes/keys, **89 verified, 0 pending, 0 in-flight**, owner-private root/cache 0700 and files 0600, only four recognized root entries and no residual lock. No individual identifiers, keys, hashes, image bytes or media references are in this report. This is author-observed remote storage/readback evidence; an **independent remote evidence review** and all remaining Stage 2 gates are separate. C04 CONAPO/SESNSP originals, G2/G3/G4, association publication decisions and retention review at 30 days remain open. No zero-cost claim is made.
